# Variables are reserved memory locations to store values. 

# declare a variabe var and assign it a value.

var = 100
print("The value of var is "+str(var))

#Re-declaring the variable var.
var = "Welcome"
print(var)